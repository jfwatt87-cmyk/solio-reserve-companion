# dependencies
node_modules/

# build output (GitHub Actions rebuilds from source)
dist/
tsconfig.tsbuildinfo

# old standalone Leaflet prototype (26 MB, not part of the app)
prototype/

# editor / OS cruft
.DS_Store
*.local
.vscode/
.idea/

# generated at build (prebuild regenerates from public/tiles/)
public/tiles-manifest.json

# local deploy artifact
solio-map-dist.zip

# NEVER commit: raw staff GPS survey (timestamped drive diary + named wildlife tracks)
# Regenerate locally via tools/gis/export_layers.sh; hash pinned in layers/MANIFEST.sha256
tools/gis/layers/SolioMappingedited_GPXtoFeatures.geojson
